import { Flex } from "antd";
import React from "react";

interface BrandPageFooterProps {}

const BrandPageFooter: React.FC<BrandPageFooterProps> = ({}) => {
  return (
    <Flex vertical gap={10}>
      footer
    </Flex>
  );
};

export default BrandPageFooter;
